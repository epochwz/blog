# 插入记录

```bash
INSERT [INTO] tbName [(colName,...)] {VALUES | VALUE} ({expr | DEFAULT | NULL})

INSERT [INTO] tbName SET [(colName,...)] SELECT ...
子查询:INSERT [INTO] tbName SET colName={expr | DEFAULT | NULL}
```