# 挂载命令

```
# 查看已挂载的设备
$ mount
# 自动挂载/etc/fstab配置的设备
$ mount -a

# 挂载命令格式
$ mount [-t 文件系统] [-o 特殊选项] 设备文件名 挂载点
# 卸载命令
$ unmount 设备文件名/挂载点

# 挂载光盘
$ mkdir /mnt/cdrom && $ mount -t iso9600 /dev/sr0 /mnt/cdrom

```